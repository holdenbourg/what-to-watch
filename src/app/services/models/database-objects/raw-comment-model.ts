export interface RawCommentModel {
    postId: string,
    profilePicture: string,
    username: string,
    comment: string,
    likes: string[],
    replies: string[],
    commentDate: string
}